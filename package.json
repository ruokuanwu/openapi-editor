{
  "name": "openapi-editor-monorepo",
  "private": true,
  "description": "VS Code Extension with Webview Monorepo",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "eslint . --ext .ts,.vue",
    "postinstall": "pnpm run build --filter @openapi-editor/shared"
  }
}